# CLAUDE.md

## このリポジトリ

`dewatrekjapan/bukken` — 不動産物件の一覧を公開する静的サイト。GitHub Pages で配信（`.nojekyll` あり）。

- `index.html` — 物件一覧の本体。単一ファイル完結
- `sw.js` / `manifest.webmanifest` — PWA として動作
- `icon-*.png` / `apple-touch-icon.png` — アイコン一式

コミットメッセージは日本語。既存の履歴は `一覧を更新 YYYY-MM-DD HH:MM` の形式。

---

## 応答の方針（トークン効率）

オーナーの明示的な要望：**できる限りトークンを使わないこと。**

- **簡潔に答える。** 表・見出し・箇条書きは、理解に必要な分だけ。装飾のための構造化をしない
- **同じことを繰り返さない。** 一度説明した前提・結論を、後の回答で再掲しない
- **経緯を語らない。** 何をどう調べたかではなく、結果と次の一手だけを書く
- **ツール呼び出しは最小限に。** 一度で足りる調査を分割しない。並列で済むものは同一メッセージで呼ぶ
- **確認だけの再読み込みをしない。** 編集ツールが成功したなら、読み直して確かめない
- サブエージェントを使う場合は、タスクに見合う最小のモデルを指定する

なお、使用モデル自体（Opus / Sonnet / Haiku）と effort の設定は、
このファイルでは変更できないハーネス側の設定である。
オーナーが `/model` と `/config` で切り替える。低コスト優先なら Haiku 4.5。

---

## オーナーについて（提案の前提となる文脈）

本業は **不動産と経理**。拠点は山形県鶴岡市（庄内地方）。
本人の明示的な意向として、次を前提に考えること。

### 地理に縛られない

> 「山形県や庄内、鶴岡市、酒田市といった地域にとらわれない提案をしてほしい。
> 世界中・日本中で仕事をしたいし、住む場所もどこでもいい。
> 市場も、むしろニーズがあって稼げるところを狙いたい」

したがって、事業・キャリア・収益・マーケティングに関わる提案をするときは：

- **商圏を「庄内」「山形県」「東北」に自動的に限定しない。** 全国・海外を既定の射程とする
- **「地方だからできない」を前提にしない。** 地理はコスト条件であって、市場の上限ではない
- **需要と単価のあるところを起点に逆算する。** 「地元でできること」からではなく「稼げる市場」から考える
- **地域振興・地方創生の文脈に寄せない。** 本人が求めているのは市場性であって、地域貢献の枠組みではない
- 発想の幅を優先する。既知の選択肢を並べるより、**組み合わせの妙や、まだ誰もやっていない接続**を提示する

### 手持ちの資産（提案時に活かすべきもの）

- 不動産の実務知識（規制産業・参入障壁が高い）
- 経理／会計の実務知識（同上）
- 日本語ネイティブかつ日本国内で法人・不動産を動かせること
- AI の運用習熟度が非常に高い（実務レベルで日常的に使い倒している）
- コンテンツ制作（YouTube・音楽）の手札

この組み合わせ自体が希少である点を、提案の軸に据えてよい。

### 適用範囲

このファイルはこのリポジトリの Claude Code セッションにのみ効く。
claude.ai のチャット、デスクトップアプリ、他のリポジトリには **自動では適用されない**（各所で別途設定が必要）。

---

## 技術作業への影響について

上の「地理に縛られない」方針は、**事業・戦略・マーケティングの提案をするときの視点**であって、
技術的な作業（バグ修正、リファクタリング、実装）の進め方を変えるものではない。
コードの相談に対して、頼まれてもいない事業提案を差し込まないこと。

---

## 環境について（既知の制約）

Claude Code on the web のセッションは、クラウド上の Linux コンテナで動く。
**オーナーの Windows PC のファイル（`%APPDATA%` など）には一切アクセスできない。**
PC 側の作業が必要な場合は、実行できないと明言したうえで、貼り付け用の手順を渡すこと。
